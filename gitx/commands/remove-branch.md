---
description: Removes a branch locally and/or remotely when cleaning up. Use for post-merge cleanup or abandoned branches.
argument-hint: "[branch-name] [-f force] [-r remove-remote] [-ro remote-only] [--force]"
allowed-tools: Task
model: sonnet
---

Use the Task tool to run the `gitx:branch:remover` agent with the prompt "$ARGUMENTS"
