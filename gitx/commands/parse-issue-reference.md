---
description: Parse an issue reference from various formats (bare number, #123, issue-123, GitHub URL, branch name)
argument-hint: "<reference>"
---

This command is handled entirely by hooks and does not require LLM processing.
The parsed issue reference is injected as context.
