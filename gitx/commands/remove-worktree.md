---
description: Removes a worktree and associated branch when cleaning up. Use for post-merge cleanup or abandoned work.
argument-hint: "[NAME] [-f force] [-r remove-remote] [--force]"
allowed-tools: Task
model: sonnet
---

Use the Task tool to run the `gitx:worktree:remover` agent with the prompt "$ARGUMENTS"
