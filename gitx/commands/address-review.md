---
description: Responds to PR review comments when feedback needs addressing. Use for iterating on pull request feedback.
argument-hint: "[--worktree <worktree>] [--resolve-level <all|critical|important>] [--force | -f] [<review-comment>]"
allowed-tools: Task
model: sonnet
---

Use the Task tool to run the `gitx:address-review:review-responder` agent with the prompt "$ARGUMENTS"
