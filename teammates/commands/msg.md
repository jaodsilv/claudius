---
description: Alias for /send-message — same args and behavior.
argument-hint: "<teammate> <message> [--subject=\"<short label>\"] [--kind=request|status|reply] [--correlation-id=<id>]"
allowed-tools: Skill
model: sonnet
---

# /msg

Alias for `/send-message`. Behavior, arguments, and semantics are
identical — see `commands/send-message.md`.

Execute `/send-message` with the same `$ARGUMENTS` verbatim.
