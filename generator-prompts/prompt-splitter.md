Given a prompt. Create a separate System Prompt and User Prompt to better perform the task.

You will be provided the following required arguments:

* {{PROMPT}}:
* {{GUIDELINES}}: Either an attachment or a copy-pasted text, optional. Defaults to your own knowledge of the topic.

Before comparing you should create an strategy of how to split it.

You should also infer the goal of this prompt based on the prompt itself

\* Separate the generated prompt into system prompt and user prompt
